import app from './app';

const port = 3001;
app.listen(process.env.APP_PORT
//   ,() => {
//   console.log(`Listening of port ${port}`);
//   console.log(`Click in http://localhost:${port}`);
// }
);
